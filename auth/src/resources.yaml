tests:
  testing: true
  track_modifications: true
  debug: true
  unit:
    db:
      uri: sqlite:///:memory?check_same_thread=False
      secret: secret
  integration:
    db:
      uri: sqlite:///:memory?check_same_thread=False
      secret: secret
    cache:
      host: localhost
      port: 6379
      reset: True
      db: 0
  functional:
    db:
      uri: postgresql://test_user:test123@localhost/meli-challenge
      secret: secret
      host: localhost
      port: 5432
      user: test_user
      password: test123
      database: meli-challenge
    cache:
      host: localhost
      port: 6379
      reset: False
      db: 1 # Separación con bases de datos de tests de integración

prod:
  testing: false
  track_modifications: false
  debug: false
  db-data:
    db:
      uri: postgresql://username:password@localhost/mydatabase
      secret: secret
      host: localhost
      port: 5432
      user: postgres
      password: postgres
      database: test